import ReactDOM from "react-dom/client";
import App from "./App";

// Talar om att root är applikationsroten för min React appliaktion
const root = ReactDOM.createRoot(document.querySelector("#root"));

root.render(<App />);
